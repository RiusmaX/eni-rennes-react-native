import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  container: {
    height: '100%',
    justifyContent: 'center'
  },
  image: {
    height: 100,
    resizeMode: 'contain',
    alignSelf: 'center'
  }
})
